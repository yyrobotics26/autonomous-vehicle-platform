rootProject.name = "autonomous-vehicle-platform"

include(
        "shared",
        "contracts",
        "services:simulator-service",
        "services:gateway-service",
        "services:telemetry-service"
)